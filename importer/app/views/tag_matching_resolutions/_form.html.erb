<%= form_for(@tag_matching_resolution) do |f| %>
  <% if @tag_matching_resolution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_matching_resolution.errors.count, "error") %> prohibited this tag_matching_resolution from being saved:</h2>

      <ul>
      <% @tag_matching_resolution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :entry_id %><br />
    <%= f.text_field :entry_id %>
  </div>
  <div class="field">
    <%= f.label :serialized_entry %><br />
    <%= f.text_area :serialized_entry %>
  </div>
  <div class="field">
    <%= f.label :resolution_type %><br />
    <%= f.text_field :resolution_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
