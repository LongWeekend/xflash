$("#search_results_<%= @exception_id %>").html("<%= escape_javascript ( select ("tag_matching_exception", "resolved_entry_id", @results.collect {|entry| [entry.human_readable, entry.id]}, {:include_blank => @include_blank}) ) %>")
$("#match_btn_<%= @exception_id %>").show()
<% if (@results.count > 0) %>
$("").hide()
<% end %>
